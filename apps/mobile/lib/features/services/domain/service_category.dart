/// Categories used to organize services in the Portal catalog.
///
/// These values belong to the frontend domain layer and do not assume how the
/// backend represents categories in JSON.
enum ServiceCategory {
  humanResources,
  leave,
  payroll,
  documents,
  profile,
  general,
}
