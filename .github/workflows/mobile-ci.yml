name: mobile-ci
on: [workflow_dispatch]
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo Configure this workflow in its implementation branch
